name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      - name: Compile TypeScript
        run: npm run compile

      - name: Package extension
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          npx @vscode/vsce@3.2.2 package --out ralph-${VERSION}.vsix

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: ralph-*.vsix
          generate_release_notes: true
